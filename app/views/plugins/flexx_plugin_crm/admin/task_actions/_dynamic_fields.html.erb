<button class="btn btn-sm btn-bold btn-yellow dropdown-toggle" data-toggle="dropdown" aria-expanded="false"><i class="ti-wand"></i></button>
<div class="dropdown-menu">
  <% @dynamic_fields.each do |df| %>
    <% if df.first == '-' %>
      <div class="dropdown-divider"></div>
    <% else %>
      <a href="#" class="dropdown-item" data-dynamic-field="<%= df.last %>"><%= df.first %></a>
    <% end %>
  <% end %>
</div>