<div id="qv-notifications" class="quickview">
  <header class="quickview-header">
    <p class="quickview-title lead">Notifications</p>
    <span class="close"><i class="ti-close"></i></span>
  </header>

  <div class="quickview-body">
    <div class="media-list media-list-hover media-list-divided media-sm">
    <% @system_feed.each do |f| %>
      <a class="media media-new" href="/admin/next/contacts/<%= '' %>">
          <span class="avatar bg-success"><i class="ti-user"></i></span>
          <div class="media-body">
            <p><%= f.message %></p>
            <time><%= time_ago_in_words(f.time) %> ago</time>
          </div>
        </a>
    <% end %>
    </div>
  </div>
  <footer class="quickview-footer flexbox">
    <div>
      <a href="#">Clear all</a>
    </div>
    <div>
      <a href="#" data-provide="tooltip" title="Update"><i class="fa fa-repeat"></i></a>
      <a href="#" data-provide="tooltip" title="Settings"><i class="fa fa-gear"></i></a>
    </div>
  </footer>
</div>
